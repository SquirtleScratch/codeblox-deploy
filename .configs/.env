# .env.template - Docker Environment Variables Template

# This file serves as a template for defining environment variables 
# that are required by Docker Compose services, particularly for the 
# PostgreSQL database used in the Codeblox project. 
# 
# To use this template, copy it to a new file named `.env` in the 
# same directory.
#
# Note: Do NOT commit the actual `.env` file to version control 
# (e.g., Git) to protect sensitive information like database 
# credentials.

# PostgreSQL database password
POSTGRES_PASSWORD=your_postgres_password_here

# PostgreSQL database username
POSTGRES_USER=your_postgres_user_here

# PostgreSQL database name
POSTGRES_DB=your_postgres_db_name_here

# Organization name or identifier
ORGANIZATION=your_organization_name_here